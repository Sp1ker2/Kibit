# 🎬 LiveKit Stream Application

Современное веб-приложение для потоковой трансляции с использованием **LiveKit WebRTC** технологии. Поддерживает администраторские и пользовательские роли, управление аккаунтами, комнатами и запись стримов.

[![LiveKit](https://img.shields.io/badge/LiveKit-2.15.14-blue)](https://livekit.io)
[![React](https://img.shields.io/badge/React-19.1.1-61dafb)](https://react.dev)
[![Node.js](https://img.shields.io/badge/Node.js-18+-green)](https://nodejs.org)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.9.3-blue)](https://www.typescriptlang.org)

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Быстрый старт](#-быстрый-старт)
- [Документация скриптов](#-документация-скриптов)
- [Установка](#-установка)
- [Запуск](#-запуск)
- [Деплой](#-деплой)
- [Структура проекта](#-структура-проекта)
- [API](#-api)
- [Администрирование](#-администрирование)
- [Решение проблем](#-решение-проблем)

---

## ✨ Возможности

### Для администраторов:
- 👥 **Управление пользователями** - создание, редактирование, удаление аккаунтов
- 🚪 **Управление комнатами** - создание приватных и публичных комнат
- 📊 **Панель мониторинга** - просмотр активных стримов и статистики
- 🔐 **Управление ролями** - назначение администраторов и пользователей
- 📹 **Запись стримов** - автоматическая запись всех трансляций

### Для пользователей:
- 📡 **Потоковая трансляция** - высококачественное видео и аудио
- 💬 **Чат в реальном времени** - общение во время трансляции
- 🎥 **Управление камерой и микрофоном** - переключение устройств на лету
- 🖥️ **Демонстрация экрана** - показ рабочего стола
- 📱 **Адаптивный интерфейс** - работает на десктопе и мобильных устройствах
- 🔒 **Приватные комнаты** - защищенные паролем трансляции

---

## 🛠 Технологии

### Frontend:
- **React 19** - пользовательский интерфейс
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **TailwindCSS** - стилизация
- **LiveKit React Components** - компоненты для WebRTC
- **Radix UI** - UI компоненты
- **Lucide React** - иконки

### Backend:
- **Node.js + Express** - API сервер
- **SQLite** (better-sqlite3) - база данных
- **LiveKit Server SDK** - интеграция с LiveKit
- **JOSE** - JWT токены
- **Multer** - загрузка файлов

### WebRTC:
- **LiveKit Server** - медиа сервер
- **LiveKit Client SDK** - клиентская библиотека

---

## 🚀 Быстрый старт

### Локальная разработка (за 2 минуты):

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd LiveKit

# 2. Первоначальная настройка
./setup.sh

# 3. Запустите приложение
./start.sh
```

**Готово!** Откройте http://localhost:5173 в браузере.

### Доступ из локальной сети:

Приложение автоматически доступно из локальной сети. После запуска `./start.sh` вы увидите IP адрес:
```
📱 Доступ из локальной сети:
   👉 http://192.168.1.100:5173
```

---

## 📚 Документация скриптов

**⭐ У проекта есть 12 оптимизированных скриптов и подробная документация!**

### Начните здесь:
1. **[НАЧНИ_ЗДЕСЬ.md](./НАЧНИ_ЗДЕСЬ.md)** - Стартовая точка
2. **[SCRIPTS_README.md](./SCRIPTS_README.md)** - Быстрая шпаргалка по скриптам
3. **[SCRIPTS_GUIDE.md](./SCRIPTS_GUIDE.md)** - Полное руководство (все детали)

### Основные скрипты:

**Локальная разработка:**
- `./setup.sh` - Первоначальная настройка
- `./start.sh` - Запуск приложения
- `./cleanup.sh` - Очистка (если что-то сломалось)

**Продакшн (один сервер):**
- `./start-simple.sh` - Простой запуск на сервере
- `./start-with-https.sh` - Запуск через daemon
- `./check-livekit.sh` - Полная диагностика

**Продакшн (несколько серверов):**
- `./deploy-to-all-servers.sh` - Деплой на все серверы
- `./start-all-servers.sh` - Запуск на всех серверах
- `./check-all-servers.sh` - Проверка всех серверов
- `./setup-load-balancer.sh` - Настройка балансировщика нагрузки

**Утилиты:**
- `./view-database.sh` - Просмотр базы данных
- `./upload-db.sh` - Загрузка БД на сервер

---

## 📥 Установка

### Требования:

**Обязательно:**
- Node.js 18+ (рекомендуется 20+)
- npm (поставляется с Node.js)
- LiveKit Server

**Установка LiveKit Server:**

**macOS:**
```bash
brew install livekit
```

**Linux:**
```bash
curl -sSL https://get.livekit.io | bash
```

**Windows:**
Скачайте с [livekit.io/download](https://livekit.io/download)

---

## ▶️ Запуск

### 1. Локальный запуск (разработка):

```bash
# Первый раз:
./setup.sh

# Каждый раз:
./start.sh
```

Приложение запустит 3 сервиса:
- **Frontend** (Vite): http://localhost:5173
- **API Server**: http://localhost:3001
- **LiveKit Server**: ws://localhost:7880

**Остановка:** Нажмите `Ctrl+C`

---

### 2. Запуск на сервере (продакшн):

#### Вариант А: Простой запуск (без systemd)

```bash
# На сервере:
cd /www/wwwroot/LiveKit
./start-simple.sh

# Проверка:
./check-livekit.sh
```

#### Вариант Б: Через daemon (панель управления хостингом)

```bash
# В панели управления создайте daemon с командой:
cd /www/wwwroot/LiveKit && ./start-with-https.sh
```

**Логи:** `/tmp/livekit.log`, `/tmp/api.log`, `/tmp/frontend.log`

---

## 🌐 Деплой

### Деплой на один сервер:

```bash
# 1. Скопируйте проект на сервер
scp -r -P <PORT> . user@server:/www/wwwroot/LiveKit/

# 2. На сервере установите зависимости
ssh user@server
cd /www/wwwroot/LiveKit
cd streamApp && npm install && cd ..

# 3. Запустите
./start-simple.sh
```

### Деплой на несколько серверов (с балансировкой):

```bash
# 1. Разверните на все серверы
./deploy-to-all-servers.sh

# 2. Запустите на всех
./start-all-servers.sh

# 3. Проверьте статус
./check-all-servers.sh

# 4. Настройте балансировщик (на главном сервере)
./setup-load-balancer.sh
```

**Подробнее:** [SCRIPTS_GUIDE.md - Сценарий 3](./SCRIPTS_GUIDE.md#сценарий-3-развертывание-на-5-серверах-с-балансировкой)

---

## 📁 Структура проекта

```
LiveKit/
├── 📄 README.md                    # Этот файл (главная документация)
├── 📖 НАЧНИ_ЗДЕСЬ.md               # Стартовая точка
├── 📖 SCRIPTS_README.md            # Быстрая шпаргалка по скриптам
├── 📖 SCRIPTS_GUIDE.md             # Полное руководство по скриптам
│
├── 🔧 Скрипты управления (12 штук)
│   ├── setup.sh                    # Первоначальная настройка
│   ├── start.sh                    # Локальный запуск
│   ├── cleanup.sh                  # Очистка
│   ├── start-simple.sh             # Запуск на сервере (простой)
│   ├── start-with-https.sh         # Запуск через daemon
│   ├── check-livekit.sh            # Диагностика сервера
│   ├── deploy-to-all-servers.sh    # Деплой на все серверы
│   ├── start-all-servers.sh        # Запуск на всех серверах
│   ├── check-all-servers.sh        # Проверка всех серверов
│   ├── setup-load-balancer.sh      # Настройка балансировщика
│   ├── view-database.sh            # Просмотр БД
│   └── upload-db.sh                # Загрузка БД на сервер
│
├── 📁 streamApp/                   # Основное приложение
│   ├── src/                        # React исходники
│   │   ├── components/             # Компоненты
│   │   │   ├── admin/              # Админ панель
│   │   │   │   ├── AccountsPage.tsx      # Управление пользователями
│   │   │   │   ├── AdminDashboard.tsx    # Админ дашборд
│   │   │   │   ├── RoomManagement.tsx    # Управление комнатами
│   │   │   │   └── RoomControls.tsx      # Контроль комнат
│   │   │   ├── user/               # Пользовательские компоненты
│   │   │   │   ├── UserStreamPage.tsx    # Страница стрима
│   │   │   │   └── UserDashboard.tsx     # Пользовательский дашборд
│   │   │   ├── LoginForm.tsx       # Форма входа
│   │   │   ├── Navbar.tsx          # Навигация
│   │   │   └── ui/                 # UI компоненты (shadcn/ui)
│   │   ├── App.tsx                 # Главный компонент
│   │   ├── main.tsx                # Точка входа
│   │   └── index.css               # Стили
│   ├── server/                     # Backend
│   │   ├── api.js                  # API сервер (Express)
│   │   └── db.js                   # Работа с БД (SQLite)
│   ├── database.db                 # База данных SQLite
│   ├── package.json                # Зависимости
│   └── vite.config.ts              # Конфигурация Vite
│
├── 📁 livekit/                     # LiveKit Server конфигурация
│   └── livekit.yaml                # Конфигурация LiveKit
│
├── 📁 recordings/                  # Записи стримов
│
├── 📁 ansible/                     # Ansible playbooks для деплоя
│   ├── playbook.yml                # Основной playbook
│   ├── inventory.ini               # Список серверов
│   ├── nginx-lb.yml                # Настройка Nginx LB
│   ├── prometheus-grafana.yml      # Мониторинг
│   └── README.md                   # Инструкции Ansible
│
└── 📁 Документация
    ├── INSTALL.md                  # Инструкция по установке
    ├── DEPLOY.md                   # Инструкция по деплою
    ├── QUICK_START.md              # Быстрый старт
    ├── CLOUDFLARE_SETUP.md         # Настройка Cloudflare
    └── DOMAIN_SETUP.md             # Настройка домена
```

---

## 🔌 API

### Эндпоинты:

**Аутентификация:**
- `POST /api/login` - Вход в систему
- `POST /api/logout` - Выход

**Пользователи:**
- `GET /api/users` - Список всех пользователей (только админ)
- `POST /api/users` - Создать пользователя (только админ)
- `PUT /api/users/:id` - Обновить пользователя (только админ)
- `DELETE /api/users/:id` - Удалить пользователя (только админ)
- `GET /api/users/me` - Текущий пользователь

**Комнаты:**
- `GET /api/rooms` - Список комнат
- `POST /api/rooms` - Создать комнату (только админ)
- `DELETE /api/rooms/:name` - Удалить комнату (только админ)
- `GET /api/rooms/:name/participants` - Участники комнаты

**LiveKit токены:**
- `POST /api/token` - Получить токен для подключения к комнате

**Аватары:**
- `POST /api/upload-avatar` - Загрузить аватар
- `GET /api/avatar/:filename` - Получить аватар

### Примеры запросов:

**Вход:**
```bash
curl -X POST http://localhost:3001/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"Admin","password":"Admin"}'
```

**Получить токен для комнаты:**
```bash
curl -X POST http://localhost:3001/api/token \
  -H "Content-Type: application/json" \
  -d '{"roomName":"my-room","participantName":"John"}'
```

---

## 👤 Учетные записи по умолчанию

После первого запуска доступны следующие аккаунты:

**Администратор:**
- Логин: `Admin`
- Пароль: `Admin`
- Права: полный доступ

**Пользователь:**
- Логин: `User`
- Пароль: `User`
- Права: просмотр и создание стримов

**Тестовый админ:**
- Логин: `b`
- Пароль: `b`
- Права: полный доступ

⚠️ **Важно:** Смените пароли по умолчанию в продакшн среде!

---

## 👨‍💼 Администрирование

### Управление пользователями:

1. Войдите как администратор
2. Перейдите в **Admin Panel** → **Accounts**
3. Создавайте, редактируйте или удаляйте пользователей
4. Назначайте роли: `admin` или `user`

### Управление комнатами:

1. **Admin Panel** → **Rooms**
2. Создавайте новые комнаты (публичные или приватные)
3. Просматривайте активные трансляции
4. Удаляйте неактивные комнаты

### Просмотр базы данных:

```bash
# Локально:
./view-database.sh

# На сервере:
ssh user@server
cd /www/wwwroot/LiveKit
./view-database.sh
```

### Бэкап базы данных:

```bash
# Скачать с сервера:
scp -P <PORT> user@server:/www/wwwroot/LiveKit/streamApp/database.db ./backup.db

# Загрузить на сервер:
./upload-db.sh
```

---

## 🔍 Решение проблем

### Проблема: Порты заняты

**Симптомы:** Ошибка "Port already in use" или "EADDRINUSE"

**Решение:**
```bash
./cleanup.sh
./start.sh
```

---

### Проблема: Frontend не запускается

**Проверка:**
```bash
cd streamApp
npm install
npm run dev
```

**Логи на сервере:**
```bash
tail -f /tmp/frontend.log
```

---

### Проблема: LiveKit не подключается

**Проверка:**
```bash
# Порт слушается?
netstat -tuln | grep 7880

# Процесс запущен?
ps aux | grep livekit-server

# На сервере - полная диагностика:
./check-livekit.sh
```

**Типичные причины:**
1. LiveKit сервер не запущен
2. Порт 7880 заблокирован firewall
3. WebSocket не настроен в Nginx
4. Неверный URL в конфигурации

**Решение для Nginx:**
```nginx
location /rtc {
    proxy_pass http://127.0.0.1:7880;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

---

### Проблема: Ошибка "Connection refused"

**Проверка firewall:**
```bash
# Проверить статус
sudo ufw status

# Открыть порты
sudo ufw allow 3001/tcp
sudo ufw allow 5173/tcp
sudo ufw allow 7880/tcp
sudo ufw allow 7880/udp
```

---

### Проблема: База данных заблокирована

**Симптомы:** "Database is locked"

**Решение:**
```bash
# Остановить все процессы
pkill -f "node server/api.js"

# Проверить права
ls -la streamApp/database.db

# Исправить права
chmod 664 streamApp/database.db
```

---

### Полная диагностика на сервере:

```bash
./check-livekit.sh
```

Покажет:
- ✅ Статус портов (5173, 3001, 7880)
- ✅ Статус процессов (livekit, vite, api)
- ✅ Статус Nginx
- ✅ Последние логи
- ✅ Проверку подключения

---

## 📊 Мониторинг

### Проверка статуса локально:

```bash
# Проверить процессы
ps aux | grep -E 'livekit|vite|node.*api'

# Проверить порты
lsof -i :5173
lsof -i :3001
lsof -i :7880
```

### Проверка статуса на сервере:

```bash
# Один сервер
./check-livekit.sh

# Все серверы
./check-all-servers.sh
```

### Просмотр логов:

```bash
# LiveKit
tail -f /tmp/livekit.log

# API
tail -f /tmp/api.log

# Frontend
tail -f /tmp/frontend.log

# Nginx (на сервере)
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log
```

---

## 🚢 Production готовность

### Перед деплоем в продакшн:

- [ ] Смените пароли по умолчанию
- [ ] Настройте SSL/TLS сертификаты
- [ ] Настройте firewall
- [ ] Настройте backup базы данных
- [ ] Настройте мониторинг (Prometheus/Grafana)
- [ ] Проверьте конфигурацию Nginx
- [ ] Настройте логирование
- [ ] Добавьте rate limiting на API
- [ ] Настройте CORS правильно
- [ ] Используйте переменные окружения для секретов

### Рекомендуемая конфигурация:

**Для высокой нагрузки:**
- Используйте балансировку нагрузки (nginx)
- Разверните на несколько серверов
- Используйте Redis для сессий
- Настройте CDN для статики

**Безопасность:**
- Используйте HTTPS везде
- Настройте CSP заголовки
- Регулярно обновляйте зависимости
- Используйте строгие пароли
- Включите rate limiting

---

## 🤝 Вклад в проект

Если хотите внести изменения:

1. Изучите документацию скриптов: [SCRIPTS_GUIDE.md](./SCRIPTS_GUIDE.md)
2. Тестируйте локально перед деплоем
3. Проверяйте все скрипты после изменений
4. Обновляйте документацию при необходимости

---

## 📞 Поддержка

**Документация:**
- [НАЧНИ_ЗДЕСЬ.md](./НАЧНИ_ЗДЕСЬ.md) - Стартовая точка
- [SCRIPTS_README.md](./SCRIPTS_README.md) - Быстрая шпаргалка
- [SCRIPTS_GUIDE.md](./SCRIPTS_GUIDE.md) - Полное руководство по скриптам
- [INSTALL.md](./INSTALL.md) - Детальная установка
- [DEPLOY.md](./DEPLOY.md) - Инструкция по деплою

**Проблемы:**
1. Проверьте раздел [Решение проблем](#-решение-проблем)
2. Запустите диагностику: `./check-livekit.sh`
3. Проверьте логи: `/tmp/livekit.log`, `/tmp/api.log`, `/tmp/frontend.log`

---

## 📜 Лицензия

Проект использует следующие технологии:
- **LiveKit** - Apache 2.0 License
- **React** - MIT License
- **Node.js** - MIT License

---

## 🎉 Благодарности

- [LiveKit](https://livekit.io) - за отличный WebRTC фреймворк
- [React](https://react.dev) - за мощный UI фреймворк
- [Vite](https://vitejs.dev) - за быстрый сборщик
- [TailwindCSS](https://tailwindcss.com) - за удобные стили

---

## 📈 Roadmap

Планируемые улучшения:
- [ ] Интеграция с внешними хранилищами (S3) для записей
- [ ] Поддержка RTMP стриминга
- [ ] Улучшенная аналитика и статистика
- [ ] Мобильное приложение (React Native)
- [ ] Интеграция с YouTube/Twitch для мультистриминга
- [ ] Автоматическая модерация чата
- [ ] Поддержка донатов

---

**Версия:** 1.0.0  
**Последнее обновление:** Ноябрь 2024  
**Статус:** Production Ready ✅

---

<div align="center">

**Сделано с ❤️ для стриминг сообщества**

[⬆ Наверх](#-livekit-stream-application)

</div>

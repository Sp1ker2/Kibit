# LiveKit Stream Application 🎬

Приложение для трансляции экрана через LiveKit с админ панелью.

## 🚀 Быстрый старт

### Запуск всего одной командой:

```bash
./start.sh
```

Это запустит:
- ✅ LiveKit сервер на `0.0.0.0:7880`
- ✅ API сервер на `0.0.0.0:3001`
- ✅ React приложение на `0.0.0.0:5173`

**🌐 Доступ из локальной сети:**
После запуска вы увидите IP адрес для доступа с других устройств в той же Wi-Fi сети.

Или узнайте свой IP вручную:
```bash
./show-ip.sh
```

### Или запустите вручную в трёх терминалах:

**Терминал 1 - LiveKit сервер:**
```bash
cd livekit
livekit-server --dev
```

**Терминал 2 - API сервер:**
```bash
cd streamApp
npm run api
```

**Терминал 3 - React приложение:**
```bash
cd streamApp
npm run dev
```

## 👥 Учетные записи

### Администратор:
- **Логин:** Admin
- **Пароль:** Admin
- Доступ к панели управления со списком стримов и записей

### Пользователь:
- **Логин:** User
- **Пароль:** User  
- Возможность стримить экран на сервер

## 📋 Использование

1. Откройте браузер на `http://localhost:5175`
2. Войдите как User / User
3. Нажмите **"Запустить стрим"**
4. Выберите окно или экран для трансляции
5. Видите превью своего стрима!

## 🛠️ Технологии

- **LiveKit** - WebRTC сервер для трансляции
- **React + TypeScript** - фронтенд
- **Vite** - сборка
- **shadcn/ui** - UI компоненты
- **Tailwind CSS** - стили

## 📦 Структура проекта

```
LiveKit/
├── livekit/          # LiveKit сервер (Go)
├── streamApp/        # React приложение
│   ├── src/
│   │   ├── components/
│   │   │   ├── admin/      # Админ панель
│   │   │   ├── user/       # Страница стрима
│   │   │   └── ui/         # UI компоненты
│   │   └── lib/            # Утилиты (токены)
│   └── package.json
├── start.sh          # Скрипт запуска
└── README.md
```

## ⚙️ Dev режим LiveKit

Сервер запускается с параметрами для разработки:
- API Key: `devkey`
- API Secret: `secret`
- WebSocket: `ws://localhost:7880`

## 🔧 Требования

- Node.js 18+
- LiveKit server (установка: `brew install livekit`)
- Современный браузер с поддержкой WebRTC

## 📝 Примечания

- В продакшене токены должны генерироваться на бэкенде
- LiveKit сервер должен быть запущен ПЕРЕД началом стрима
- Для трансляции нужно разрешить доступ к экрану в браузере

## 🌐 Доступ из локальной сети

Приложение автоматически доступно для всех устройств в вашей локальной Wi-Fi сети!

**Как подключиться с телефона/планшета:**
1. Запустите `./start.sh` на компьютере
2. Найдите IP адрес (он будет показан при запуске)
3. На другом устройстве в той же Wi-Fi откройте браузер
4. Введите `http://ВАШ_IP:5173`

Подробнее: [NETWORK_ACCESS.md](./NETWORK_ACCESS.md)


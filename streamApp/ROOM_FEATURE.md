# 🏠 Система комнат - Новая функция

## ✨ Что добавлено:

### 1. **Выбор комнаты при входе**
При входе в систему пользователь теперь должен:
1. Выбрать комнату из выпадающего списка
2. Ввести логин
3. Ввести пароль

### 2. **База данных обновлена**
- Добавлена таблица `rooms` с комнатами
- У пользователей теперь есть поле `room_name` - последняя выбранная комната
- По умолчанию созданы 3 комнаты: "Комната 1", "Комната 2", "Комната 3"

### 3. **Отображение комнаты**
- **Для пользователей**: комната отображается в заголовке рядом с именем
- **Для админов**: в списке аккаунтов показывается последняя комната пользователя

## 📝 Структура БД:

### Таблица `rooms`:
```sql
CREATE TABLE rooms (
  id INTEGER PRIMARY KEY,
  name TEXT UNIQUE NOT NULL,
  description TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
)
```

### Таблица `users` (обновлена):
```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY,
  username TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  role TEXT DEFAULT 'user',
  room_name TEXT,  -- ⭐ НОВОЕ ПОЛЕ
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
)
```

## 🚀 Как использовать:

### Для пользователей:
1. Откройте приложение
2. Выберите комнату из списка
3. Введите логин и пароль
4. Нажмите "Войти"
5. В заголовке будет показана ваша комната: `📍 Комната 1`

### Для администраторов:

#### Управление комнатами:
API эндпоинты уже созданы:
- `GET /api/room-list` - получить все комнаты
- `POST /api/room-list` - создать комнату
  ```json
  { "name": "Комната 4", "description": "Описание" }
  ```
- `DELETE /api/room-list/:id` - удалить комнату

#### Просмотр комнат пользователей:
В разделе "Аккаунты" рядом с каждым пользователем отображается бейдж с последней выбранной комнатой.

## 🔄 Миграция существующих данных:

Если у вас уже есть старая база данных `database.db`:

### Вариант 1: Автоматическая миграция (рекомендуется)
Просто запустите сервер - SQLite автоматически добавит новое поле `room_name`:
```bash
cd streamApp
npm run dev
```

### Вариант 2: Пересоздать БД
```bash
cd streamApp
rm database.db  # Удаляет старую БД
npm run dev     # Создает новую БД с комнатами
```

⚠️ **Внимание**: При удалении БД все пользователи будут удалены! Останутся только Admin/Admin и User/User.

## 📂 Измененные файлы:

1. **База данных**:
   - `streamApp/server/db.js` - добавлено поле room_name, таблица rooms

2. **Backend API**:
   - `streamApp/server/api.js` - обновлен /api/auth/login для сохранения комнаты

3. **Frontend**:
   - `streamApp/src/App.tsx` - сохранение комнаты в localStorage
   - `streamApp/src/components/LoginForm.tsx` - выпадающий список комнат
   - `streamApp/src/components/user/UserStreamPage.tsx` - отображение комнаты
   - `streamApp/src/components/admin/AccountsPage.tsx` - показ комнаты в списке

## 💡 Примечания:

- Выбранная комната сохраняется в БД при каждом входе
- Комната сохраняется в localStorage для сессии
- При выходе комната очищается из localStorage
- Админы могут создавать/удалять комнаты через API

## 🔧 Добавление новых комнат:

Через API:
```bash
curl -X POST http://localhost:3001/api/room-list \
  -H "Content-Type: application/json" \
  -d '{"name": "Новая комната", "description": "Описание комнаты"}'
```

Или прямо в БД:
```bash
sqlite3 streamApp/database.db
INSERT INTO rooms (name, description) VALUES ('Комната 4', 'Четвертая комната');
```

## ✅ Готово!

Система комнат полностью интегрирована и готова к использованию!


# 🚀 Развертывание через Ansible (Рекомендуется)

Ansible намного лучше подходит для развертывания на множестве серверов, чем bash скрипты.

## ✅ Преимущества Ansible

1. **Идипотентность** - можно запускать много раз, результат одинаковый
2. **Надежность** - лучшая обработка ошибок
3. **Читаемость** - YAML легче читать чем bash
4. **Встроенные модули** - много готовых модулей для работы с системой
5. **Инвентаризация** - удобное управление списком серверов
6. **Параллелизм** - выполняет задачи параллельно на всех серверах
7. **Откат изменений** - можно откатить изменения

## 📦 Установка Ansible

### macOS

```bash
brew install ansible
```

### Linux

```bash
sudo apt-get update
sudo apt-get install ansible
```

### Или через pip

```bash
pip install ansible
```

## 🚀 Быстрый старт

### 1. Перейдите в директорию ansible

```bash
cd ansible
```

### 2. Разверните на всех серверах

```bash
ansible-playbook playbook.yml
```

### 3. Настройте Nginx Load Balancer

```bash
ansible-playbook nginx-lb.yml
```

### 4. Проверьте статус

```bash
make status
# или
ansible all -m shell -a "netstat -tuln | grep -E '3001|5173|7880'"
```

## 📝 Использование Makefile (удобнее)

```bash
make deploy    # Развернуть на всех серверах
make nginx     # Настроить Load Balancer
make check     # Проверить подключение
make status    # Показать статус
make logs      # Показать логи
make restart   # Перезапустить сервисы
```

## 🔧 Детальная инструкция

См. `ansible/README.md` для полной документации.

## 🆚 Сравнение с bash скриптами

| Характеристика | Bash скрипты | Ansible |
|---|---|---|
| Идипотентность | ❌ Нет | ✅ Да |
| Обработка ошибок | ⚠️ Ручная | ✅ Автоматическая |
| Читаемость | ⚠️ Средняя | ✅ Высокая |
| Параллелизм | ❌ Нет | ✅ Да |
| Откат изменений | ❌ Нет | ✅ Да |
| Встроенные модули | ❌ Нет | ✅ Да |
| Инвентаризация | ⚠️ Ручная | ✅ Встроенная |

## 🎯 Рекомендация

**Используйте Ansible для развертывания!** Это стандарт индустрии для оркестрации серверов.


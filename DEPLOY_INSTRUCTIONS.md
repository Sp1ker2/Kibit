# 📋 Инструкция для деплоя viewer.html

## 🔧 Автоматический деплой не работает (проблема с SSH подключением)

Выполни следующие команды вручную:

### Вариант 1: Через SSH терминал

```bash
# 1. Подключись к серверу
ssh -p 16205 root@195.133.17.131

# 2. После подключения, скопируй исправленный viewer.html
# (используй другой терминал или scp)

# Или выполни через scp из локального терминала:
scp -P 16205 python-streaming/simple-stream-server/public/viewer.html root@195.133.17.131:/root/simple-stream-server/public/viewer.html

# 3. На сервере перезапусти сервер
cd /root/simple-stream-server
pkill -f "node server.js"
nohup node server.js > /tmp/simple-stream.log 2>&1 &

# 4. Проверь логи
tail -f /tmp/simple-stream.log
```

### Вариант 2: Прямое копирование через scp

```bash
# Из локальной директории проекта
cd /Users/bogdanprihodko/Downloads/LiveKit

# Скопируй файл
scp -P 16205 \
    python-streaming/simple-stream-server/public/viewer.html \
    root@195.133.17.131:/root/simple-stream-server/public/viewer.html

# Перезапусти сервер
ssh -p 16205 root@195.133.17.131 << 'EOF'
cd /root/simple-stream-server
pkill -f "node server.js" || true
sleep 2
nohup node server.js > /tmp/simple-stream.log 2>&1 &
echo "✅ Сервер перезапущен!"
EOF
```

### Вариант 3: Через веб-интерфейс (если есть)

Если у тебя есть доступ к файловой системе сервера через веб-интерфейс:
1. Открой `/root/simple-stream-server/public/viewer.html`
2. Замени содержимое на исправленную версию из `python-streaming/simple-stream-server/public/viewer.html`
3. Перезапусти сервер

## ✅ После деплоя проверь:

1. Открой `https://kibitkostreamappv.pp.ua:8443/viewer.html?room=Azov_2&user=Стрімер_1`
2. Подключи второго стримера к той же комнате
3. Убедись, что первый стример не отключается при подключении второго

## 🔍 Что было исправлено:

- Исправлена проблема с переподключением при получении `summary` сообщений
- Теперь viewer не переподключается, если текущий стример все еще активен
- Поддержка мультистрима (несколько стримеров в одной комнате)


# 📋 ПРОМПТ: СИТУАЦИЯ И ПРОБЛЕМЫ ПРОЕКТА

## 🎯 КОНТЕКСТ ПРОЕКТА

**Проект:** LiveKit Stream App с Google Drive интеграцией
**Домен:** kibitkostreamappv.pp.ua
**Сервер:** 195.133.39.41 (Ubuntu 22.04)
**Панель управления:** aaPanel/btPanel

---

## 📁 АРХИТЕКТУРА ПРОЕКТА

### Структура файлов на сервере:

```
/www/wwwroot/LiveKit/
├── streamApp/
│   ├── server/
│   │   ├── api.js                    # API сервер (порт 3001)
│   │   └── storage/
│   │       └── drive.js              # Google Drive интеграция
│   └── public/
│       └── videos.html               # Статическая страница для просмотра видео
│
├── simple-stream-server/
│   └── public/
│       └── viewer.html               # Клиент для просмотра стримов
│
└── recordings/                       # Локальная папка для записей (NFS)

/www/server/panel/
├── webserver/
│   ├── conf/
│   │   └── webserver.conf            # Главный конфиг Nginx
│   └── logs/
│       └── error.log                 # Логи ошибок
│
└── vhost/nginx/
    └── kibitkostreamappv.pp.ua.conf  # Конфиг домена
```

---

## 🔧 КОНФИГУРАЦИЯ

### 1. API Сервер (Node.js)
- **Файл:** `/www/wwwroot/LiveKit/streamApp/server/api.js`
- **Порт:** 3001
- **Статус:** ✅ Работает
- **Эндпоинты:**
  - `GET /api/health` - проверка работоспособности
  - `GET /api/drive/rooms` - список комнат из Google Drive
  - `GET /api/drive/rooms/:roomId/users` - список пользователей
  - `GET /api/drive/users/:userId/dates` - список дат
  - `GET /api/drive/dates/:dateId/videos` - список видео
  - `GET /api/drive/files/:fileId/stream` - ссылка для просмотра видео

### 2. Nginx (Веб-сервер)
- **Конфиг домена:** `/www/server/panel/vhost/nginx/kibitkostreamappv.pp.ua.conf`
- **Главный конфиг:** `/www/server/panel/webserver/conf/webserver.conf`
- **Порты:** 80, 8443, 31966
- **Статус:** ⚠️ Проблемы с маршрутизацией

### 3. Конфигурация Nginx для домена:

```nginx
server {
    listen 80;
    listen 8443 ssl http2;
    listen 31966 ssl http2;
    server_name kibitkostreamappv.pp.ua;

    ssl_certificate /www/server/panel/ssl/certificate.pem;
    ssl_certificate_key /www/server/panel/ssl/privateKey.pem;

    # Статический файл videos.html
    location = /videos.html {
        root /www/wwwroot/LiveKit/streamApp/public;
        try_files /videos.html =404;
        add_header Content-Type "text/html; charset=utf-8";
    }

    # API проксирование
    location ^~ /api {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        client_max_body_size 500M;
    }

    # LiveKit WebSocket
    location /rtc {
        proxy_pass http://127.0.0.1:7880;
        # ... WebSocket настройки
    }

    # Frontend (Vite) - всё остальное
    location / {
        proxy_pass http://127.0.0.1:5173;
        # ... настройки прокси
    }
}
```

### 4. Включение конфига домена в webserver.conf:

```nginx
http {
    # ... другие настройки
    
    # Include domain configuration
    include /www/server/panel/vhost/nginx/kibitkostreamappv.pp.ua.conf;
}
```

---

## ❌ ТЕКУЩИЕ ПРОБЛЕМЫ

### Проблема 1: 404 Not Found на всех URL
**Симптомы:**
- `https://kibitkostreamappv.pp.ua:8443/videos.html` → 404
- `https://kibitkostreamappv.pp.ua:8443/api/health` → 404
- `https://kibitkostreamappv.pp.ua:31966/videos.html` → 404

**Возможные причины:**
1. Nginx не загружает конфиг домена правильно
2. Порты не слушают правильно
3. Проблемы с SSL сертификатами
4. Маршрутизация location блоков работает неправильно
5. Файл videos.html не найден по указанному пути

### Проблема 2: Nginx конфигурация
- Конфиг домена должен быть включен в `webserver.conf`
- Включение должно быть внутри `http` блока
- Нельзя включать все `.conf` файлы (`*.conf`) - только конкретный домен

### Проблема 3: Пути к файлам
- `videos.html` находится в `/www/wwwroot/LiveKit/streamApp/public/videos.html`
- В конфиге указан `root /www/wwwroot/LiveKit/streamApp/public;`
- Проверить правильность пути и доступность файла

---

## 🔍 ЧТО НУЖНО ПРОВЕРИТЬ

### 1. Проверка процессов и портов:
```bash
# Процессы Nginx
ps aux | grep nginx | grep -v grep

# Прослушиваемые порты
netstat -tlnp | grep -E ":80|:8443|:31966"
# или
ss -tlnp | grep -E ":80|:8443|:31966"

# Процессы API сервера
ps aux | grep "server/api.js" | grep -v grep
```

### 2. Проверка конфигурации:
```bash
# Проверка конфига домена
cat /www/server/panel/vhost/nginx/kibitkostreamappv.pp.ua.conf

# Проверка включения в webserver.conf
grep -A 2 -B 2 "kibitkostreamappv\|vhost.*nginx" /www/server/panel/webserver/conf/webserver.conf

# Проверка логов ошибок
tail -20 /www/server/panel/webserver/logs/error.log
```

### 3. Локальные тесты:
```bash
# Тест videos.html
curl -k -v https://localhost:8443/videos.html -H "Host: kibitkostreamappv.pp.ua"

# Тест API
curl -k -v https://localhost:8443/api/health -H "Host: kibitkostreamappv.pp.ua"

# Прямой доступ к API
curl http://localhost:3001/api/health
```

### 4. Проверка файлов:
```bash
# Существование videos.html
ls -la /www/wwwroot/LiveKit/streamApp/public/videos.html

# Права доступа
stat /www/wwwroot/LiveKit/streamApp/public/videos.html
```

---

## ✅ ЧТО НУЖНО СДЕЛАТЬ

### Шаг 1: Диагностика
1. Проверить статус Nginx (процессы, порты)
2. Проверить логи ошибок Nginx
3. Проверить конфигурацию Nginx
4. Проверить доступность файлов
5. Проверить работу API сервера напрямую

### Шаг 2: Исправление конфигурации
1. Убедиться что конфиг домена включен в `webserver.conf`
2. Проверить правильность путей в `location` блоках
3. Проверить SSL сертификаты
4. Проверить приоритет `location` блоков (точное совпадение `=` имеет высший приоритет)

### Шаг 3: Перезагрузка и проверка
1. Перезагрузить Nginx (через `kill -HUP` или панель)
2. Проверить логи на ошибки
3. Протестировать доступность через curl локально
4. Протестировать через браузер

### Шаг 4: Проверка внешнего доступа
1. Проверить firewall правила (порты 80, 8443, 31966 открыты?)
2. Проверить DNS (домен указывает на правильный IP?)
3. Проверить SSL сертификаты (валидны ли?)

---

## 📝 ДЕТАЛИ О ФАЙЛАХ

### videos.html
- **Путь:** `/www/wwwroot/LiveKit/streamApp/public/videos.html`
- **Размер:** ~23KB
- **Содержимое:** Статический HTML с inline CSS и JavaScript
- **API URL:** Использует `window.location.origin` для запросов к API
- **Функционал:** 
  - Навигация по папкам Google Drive (комнаты → пользователи → даты → видео)
  - Поиск
  - Просмотр видео
  - Скачивание видео

### api.js
- **Путь:** `/www/wwwroot/LiveKit/streamApp/server/api.js`
- **Порт:** 3001
- **Статус:** ✅ Работает локально
- **Google Drive:** Включен (GOOGLE_DRIVE_ENABLED=true)
- **БД:** PostgreSQL

### Конфиг домена
- **Путь:** `/www/server/panel/vhost/nginx/kibitkostreamappv.pp.ua.conf`
- **Статус:** ✅ Существует и правильно настроен
- **Проблема:** Возможно не включен правильно в webserver.conf

---

## 🔑 КЛЮЧЕВЫЕ МОМЕНТЫ

1. **Nginx управляется через aaPanel/btPanel** - конфиги могут перезаписываться панелью
2. **Конфиг домена должен быть включен ВНУТРИ http блока** в webserver.conf
3. **Нельзя включать все .conf файлы** (`*.conf`) - только конкретный домен
4. **Порты 80, 8443, 31966** должны слушать Nginx
5. **Файл videos.html** должен быть доступен по пути `/www/wwwroot/LiveKit/streamApp/public/videos.html`
6. **API сервер работает на порту 3001** и должен быть доступен через прокси `/api`

---

## 🎯 ЦЕЛЬ

Добиться работоспособности следующих URL:
- ✅ `https://kibitkostreamappv.pp.ua:8443/videos.html` - открывается страница с видео
- ✅ `https://kibitkostreamappv.pp.ua:8443/api/health` - возвращает JSON с статусом
- ✅ `https://kibitkostreamappv.pp.ua:8443/api/drive/rooms` - возвращает список комнат из Google Drive

---

## 📞 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

- **SSH:** `root@195.133.39.41` (пароль: EReAGUNX)
- **Панель:** `https://195.133.39.41:8888`
- **Google Drive:** Интеграция настроена, работает локально через API
- **Логи API:** `/var/log/streamapp/api.log`
- **Логи Nginx:** `/www/server/panel/webserver/logs/error.log`

---

**Дата создания промпта:** 2025-11-14
**Последняя диагностика:** Проверка портов и конфигурации показала что Nginx слушает порты, но возвращает 404 на все запросы




⚠️ ИСПРАВЛЕНИЕ ОШИБКИ ПОДКЛЮЧЕНИЯ К LIVEKIT:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ШАГ 1: Загрузите обновленные файлы на сервер

Файлы для загрузки:
1. streamApp/src/config.ts
2. streamApp/src/components/user/UserStreamPage.tsx

Путь на сервере:
/www/wwwroot/LiveKit/streamApp/src/config.ts
/www/wwwroot/LiveKit/streamApp/src/components/user/UserStreamPage.tsx

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ШАГ 2: Проверьте что LiveKit запущен на сервере

Выполните на сервере:

netstat -tulpn | grep 7880

Или:

ss -tulpn | grep 7880

Если порта НЕТ - LiveKit не запущен!

Запустите:
cd /www/wwwroot/LiveKit
./start-with-https.sh

Или через daemon в панели:
- Откройте daemon "test"
- Остановите и запустите заново

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ШАГ 3: Проверьте логи LiveKit на сервере

tail -f /tmp/livekit.log

Или в панели нажмите "Log" на daemon.

Должны быть сообщения о запуске сервера.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ШАГ 4: Проверьте Nginx конфигурацию для WebSocket

Проверьте конфиг Nginx:

cat /etc/nginx/sites-available/kibitkostreamappv.pp.ua

Должны быть блоки:

location /rtc {
    proxy_pass http://127.0.0.1:7880;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}

И для HTTPS:

location /rtc {
    proxy_pass http://127.0.0.1:7880;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

Если блоков НЕТ - добавьте их!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ШАГ 5: После исправлений

1. Перезапустите daemon в панели
2. Перезагрузите Nginx:
   sudo nginx -t
   sudo systemctl reload nginx
3. Подождите 10 секунд
4. Проверьте порты: netstat -tulpn | grep -E '5173|3001|7880'
5. Обновите страницу в браузере (Ctrl+F5)
6. Откройте консоль (F12) и проверьте логи подключения

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ШАГ 6: Проверьте в браузере

1. Откройте консоль (F12)
2. Проверьте логи подключения:
   - Должен быть лог "🌐 Конфигурация:" с URL
   - Должен быть лог "Подключаемся к серверу: wss://..."
   - Если есть ошибка - скопируйте её текст

3. Проверьте Network tab:
   - Должен быть WebSocket подключение к /rtc
   - Статус должен быть 101 Switching Protocols

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ГЛАВНОЕ: Убедитесь что все три порта слушаются!

5173 - Frontend (Vite)
3001 - API
7880 - LiveKit

Проверка: netstat -tulpn | grep -E '5173|3001|7880'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


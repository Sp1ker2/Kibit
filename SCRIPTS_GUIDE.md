# 📚 Руководство по скриптам LiveKit Stream App

Полное руководство по всем скриптам проекта с подробным описанием их назначения и использования.

---

## 📋 Содержание

1. [Локальная разработка](#-локальная-разработка)
2. [Развертывание на сервере](#-развертывание-на-сервере)
3. [Управление несколькими серверами](#-управление-несколькими-серверами)
4. [Диагностика и мониторинг](#-диагностика-и-мониторинг)
5. [Утилиты](#-утилиты)

---

## 🏠 Локальная разработка

### `setup.sh` - Первоначальная настройка проекта

**Когда использовать:** При первом клонировании проекта на локальную машину.

**Что делает:**
- Проверяет установку Node.js, npm и livekit-server
- Устанавливает npm зависимости в директории `streamApp`
- Создает папку `recordings` для хранения записей
- Проверяет базу данных

**Использование:**
```bash
./setup.sh
```

**Требования:**
- Node.js (версия 18+)
- npm
- livekit-server (macOS: `brew install livekit`, Linux: `curl -sSL https://get.livekit.io | bash`)

---

### `start.sh` - Запуск приложения локально

**Когда использовать:** Для запуска приложения на локальной машине в режиме разработки.

**Что делает:**
- Проверяет наличие livekit-server
- Устанавливает зависимости если нужно
- Запускает LiveKit сервер (порт 7880)
- Запускает API сервер (порт 3001)
- Запускает React приложение (порт 5173)
- Показывает локальный IP для доступа из сети

**Использование:**
```bash
./start.sh
```

**Доступ:**
- Frontend: http://localhost:5173
- API: http://localhost:3001
- LiveKit: ws://localhost:7880

**Остановка:** Нажмите `Ctrl+C`

---

### `cleanup.sh` - Очистка и исправление проблем

**Когда использовать:** Когда порты заняты или что-то работает неправильно.

**Что делает:**
- Останавливает все процессы (livekit, api, vite)
- Освобождает порты 5173, 3001, 7880, 7881, 7882
- Исправляет права доступа на node_modules и скрипты

**Использование:**
```bash
./cleanup.sh
./start.sh  # После очистки запустите снова
```

---

## 🚀 Развертывание на сервере

### `start-simple.sh` - Запуск на продакшн сервере (простой)

**Когда использовать:** Для запуска на одном сервере через SSH (без systemd/daemon).

**Что делает:**
- Останавливает старые процессы
- Запускает LiveKit, API и Frontend в фоновом режиме
- Создает логи в `/tmp/livekit.log`, `/tmp/api.log`, `/tmp/frontend.log`
- Проверяет что порты слушаются

**Использование:**
```bash
# На сервере
cd /www/wwwroot/LiveKit
./start-simple.sh

# Проверить логи
tail -f /tmp/frontend.log
tail -f /tmp/api.log
tail -f /tmp/livekit.log
```

---

### `start-with-https.sh` - Запуск на продакшн через daemon

**Когда использовать:** Для запуска через панель управления хостингом (daemon).

**Что делает:**
- Останавливает старые процессы
- Запускает все сервисы в фоне
- Очищает и создает логи
- Держит скрипт запущенным (wait)
- Показывает статус портов

**Использование:**
```bash
# Через панель управления хостингом:
# 1. Создайте daemon с командой:
cd /www/wwwroot/LiveKit && ./start-with-https.sh

# Или вручную на сервере:
cd /www/wwwroot/LiveKit
./start-with-https.sh
```

**Логи:**
- `/tmp/livekit.log`
- `/tmp/api.log`
- `/tmp/frontend.log`

---

## 🌐 Управление несколькими серверами

### Список серверов в проекте:

```
195.133.17.131 (root:iFG02M6Z:16205)    # Основной сервер
195.133.17.179 (root:kSE2oBmk:35614)
195.133.39.17  (root:66AMoRNN:13845)
195.133.39.33  (root:vHdUm7B2:42460)
195.133.39.41  (root:EReAGUNX:31966)
```

---

### `deploy-to-all-servers.sh` - Развертывание на все серверы

**Когда использовать:** Для первого развертывания или полного обновления всех серверов.

**Что делает:**
- Копирует весь проект на все 5 серверов
- Устанавливает npm зависимости на каждом сервере
- Копирует базу данных на основной сервер (195.133.17.131)
- Останавливает старые процессы
- Устанавливает права доступа

**Использование:**
```bash
./deploy-to-all-servers.sh
```

**Требования:**
- `sshpass` (macOS: `brew install hudochenkov/sshpass/sshpass`, Linux: `sudo apt-get install sshpass`)

**Что происходит:**
1. Копирует `streamApp/`, `livekit/`, скрипты на каждый сервер
2. Запускает `npm install` на каждом сервере
3. Создает необходимые директории
4. Останавливает старые процессы

---

### `start-all-servers.sh` - Запуск сервисов на всех серверах

**Когда использовать:** После развертывания, чтобы запустить приложение на всех серверах.

**Что делает:**
- Подключается к каждому серверу по SSH
- Останавливает старые процессы
- Запускает LiveKit, API и Frontend на каждом сервере
- Проверяет что порты слушаются
- Показывает статус каждого сервера

**Использование:**
```bash
./start-all-servers.sh
```

**Результат:**
- Все 5 серверов работают независимо
- Каждый слушает порты: 5173, 3001, 7880

---

### `check-all-servers.sh` - Проверка статуса всех серверов

**Когда использовать:** Для мониторинга состояния всех серверов.

**Что делает:**
- Проверяет доступность каждого сервера
- Показывает какие порты слушаются
- Показывает запущенные процессы
- Показывает последние 5 строк из логов

**Использование:**
```bash
./check-all-servers.sh
```

**Пример вывода:**
```
Проверка 195.133.17.131:16205...
✅ Сервер доступен
Порты:
tcp        0      0 0.0.0.0:5173           0.0.0.0:*               LISTEN
tcp        0      0 0.0.0.0:3001           0.0.0.0:*               LISTEN
tcp        0      0 0.0.0.0:7880           0.0.0.0:*               LISTEN
...
```

---

### `setup-load-balancer.sh` - Настройка Nginx Load Balancer

**Когда использовать:** После развертывания на все серверы, для настройки балансировки нагрузки.

**Что делает:**
- Создает конфигурацию Nginx для балансировки нагрузки
- Настраивает 3 upstream: api_backend, frontend_backend, livekit_backend
- Использует `least_conn` для API и `ip_hash` для Frontend и LiveKit
- Создает конфигурацию для HTTP и HTTPS

**Использование:**
```bash
# 1. Запустите скрипт локально (создаст /tmp/nginx-lb.conf)
./setup-load-balancer.sh

# 2. На главном сервере (195.133.17.131):
sudo cp /tmp/nginx-lb.conf /etc/nginx/sites-available/streamapp-lb
sudo ln -sf /etc/nginx/sites-available/streamapp-lb /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# 3. Откройте порты на всех серверах:
sudo ufw allow 3001/tcp
sudo ufw allow 5173/tcp
sudo ufw allow 7880/tcp
sudo ufw allow 7880/udp
```

**Домен:** kibitkostreamappv.pp.ua

---

## 🔍 Диагностика и мониторинг

### `check-livekit.sh` - Полная диагностика LiveKit

**Когда использовать:** Для проверки всех компонентов системы на одном сервере.

**Что делает:**
- ✅ Проверяет порты (5173, 3001, 7880)
- ✅ Проверяет процессы (livekit-server, vite, node api.js)
- ✅ Проверяет Nginx (статус, конфигурация, location /rtc)
- ✅ Показывает последние логи
- ✅ Проверяет подключение к LiveKit

**Использование:**
```bash
# На сервере:
cd /www/wwwroot/LiveKit
./check-livekit.sh
```

**Пример вывода:**
```
🔍 Проверка состояния LiveKit...

📊 Проверка портов:
Порт 5173 (Frontend): ✅ Слушается
Порт 3001 (API): ✅ Слушается
Порт 7880 (LiveKit): ✅ Слушается

📋 Проверка процессов:
LiveKit сервер: ✅ Запущен (PID: 12345)
Vite (Frontend): ✅ Запущен (PID: 12346)
API сервер: ✅ Запущен (PID: 12347)

🌐 Проверка Nginx:
✅ Nginx запущен
✅ Конфигурация корректна
✅ Location /rtc найден в конфигурации
```

---

### `check-services.sh` - Проверка сервисов

**Когда использовать:** Быстрая проверка команд для сервера.

**Что делает:**
- Показывает команды для проверки процессов
- Показывает команды для проверки портов
- Показывает команды для просмотра логов

**Использование:**
```bash
./check-services.sh
```

---

## 🛠 Утилиты

### `view-database.sh` - Просмотр базы данных

**Когда использовать:** Для просмотра содержимого SQLite базы данных.

**Что делает:**
- Показывает структуру таблицы users
- Показывает всех пользователей
- Показывает статистику

**Использование:**
```bash
./view-database.sh
```

**Требования:**
- `sqlite3` (Linux: `sudo apt-get install sqlite3`)

**Пример вывода:**
```
📊 Просмотр базы данных SQLite

📋 Структура таблицы users:
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  role TEXT DEFAULT 'user',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

👥 Пользователи в базе данных:
id  username  password  created_at
--  --------  --------  --------------------
1   Admin     Admin     2024-01-01 00:00:00
2   User      User      2024-01-01 00:00:00
3   b         b         2024-01-01 00:00:00

📊 Статистика:
Всего пользователей: 3
```

---

### `upload-db.sh` - Загрузка базы данных на сервер

**Когда использовать:** Для обновления базы данных на сервере.

**Что делает:**
- Копирует локальную базу данных на сервер
- Устанавливает правильные права доступа

**Использование:**
```bash
./upload-db.sh
```

---

## 📝 Типичные сценарии использования

### Сценарий 1: Первый запуск локально

```bash
# 1. Клонируйте проект
git clone <repository>
cd LiveKit

# 2. Первоначальная настройка
./setup.sh

# 3. Запустите приложение
./start.sh

# 4. Откройте в браузере
open http://localhost:5173
```

**Логин по умолчанию:**
- Админ: `Admin` / `Admin`
- Пользователь: `User` / `User`

---

### Сценарий 2: Развертывание на одном сервере

```bash
# 1. Скопируйте проект на сервер (вручную или через scp)
scp -r -P <PORT> streamApp livekit *.sh user@server:/www/wwwroot/LiveKit/

# 2. Подключитесь к серверу
ssh -p <PORT> user@server

# 3. Перейдите в директорию
cd /www/wwwroot/LiveKit

# 4. Установите зависимости
cd streamApp && npm install && cd ..

# 5. Запустите приложение
./start-simple.sh

# 6. Проверьте статус
./check-livekit.sh
```

---

### Сценарий 3: Развертывание на 5 серверах с балансировкой

```bash
# 1. Разверните на все серверы
./deploy-to-all-servers.sh

# 2. Запустите на всех серверах
./start-all-servers.sh

# 3. Проверьте статус
./check-all-servers.sh

# 4. Настройте load balancer на главном сервере
./setup-load-balancer.sh

# 5. Настройте Nginx на главном сервере (195.133.17.131)
# Следуйте инструкциям из вывода setup-load-balancer.sh
```

---

### Сценарий 4: Диагностика проблем

```bash
# Если что-то не работает на сервере:

# 1. Проверьте статус
./check-livekit.sh

# 2. Посмотрите логи
tail -f /tmp/livekit.log
tail -f /tmp/api.log
tail -f /tmp/frontend.log

# 3. Проверьте порты
netstat -tuln | grep -E '3001|5173|7880'

# 4. Перезапустите сервисы
pkill -f livekit-server
pkill -f "npm run api"
pkill -f vite
./start-simple.sh

# 5. Проверьте Nginx
sudo nginx -t
sudo systemctl status nginx
sudo systemctl reload nginx
```

---

### Сценарий 5: Обновление кода на серверах

```bash
# 1. Обновите код локально
git pull

# 2. Разверните на все серверы
./deploy-to-all-servers.sh

# 3. Перезапустите сервисы
./start-all-servers.sh

# 4. Проверьте что все работает
./check-all-servers.sh
```

---

## 🔐 Информация о безопасности

**⚠️ ВАЖНО:** В скриптах есть жестко закодированные пароли и IP адреса серверов.

**Не публикуйте эти скрипты в открытых репозиториях!**

Рекомендуется:
1. Добавить файлы с паролями в `.gitignore`
2. Использовать переменные окружения для хранения паролей
3. Использовать SSH ключи вместо паролей

---

## 🆘 Частые проблемы и решения

### Проблема: Порты заняты

**Решение:**
```bash
./cleanup.sh
./start.sh
```

---

### Проблема: Frontend не запускается

**Проверьте:**
```bash
cd streamApp
npm install
npm run dev
```

**Посмотрите логи:**
```bash
tail -f /tmp/frontend.log
```

---

### Проблема: LiveKit не подключается

**Проверьте:**
1. Порт 7880 слушается: `netstat -tuln | grep 7880`
2. Nginx настроен: `grep "location /rtc" /etc/nginx/sites-available/*`
3. WebSocket работает: проверьте консоль браузера

---

### Проблема: Ошибка "Connection refused"

**Решение:**
1. Проверьте что сервисы запущены: `./check-livekit.sh`
2. Проверьте firewall: `sudo ufw status`
3. Откройте нужные порты:
```bash
sudo ufw allow 3001/tcp
sudo ufw allow 5173/tcp
sudo ufw allow 7880/tcp
```

---

## 📞 Контакты и поддержка

Если возникли проблемы:
1. Проверьте логи: `/tmp/livekit.log`, `/tmp/api.log`, `/tmp/frontend.log`
2. Запустите диагностику: `./check-livekit.sh`
3. Проверьте все серверы: `./check-all-servers.sh`

---

## 📌 Полезные команды

### На сервере:

```bash
# Проверить процессы
ps aux | grep -E 'livekit|vite|node.*api'

# Проверить порты
netstat -tuln | grep -E '3001|5173|7880'
# или
ss -tuln | grep -E '3001|5173|7880'

# Убить все процессы
pkill -f livekit-server
pkill -f "npm run api"
pkill -f vite

# Просмотр логов в реальном времени
tail -f /tmp/livekit.log
tail -f /tmp/api.log
tail -f /tmp/frontend.log

# Проверить Nginx
sudo nginx -t
sudo systemctl status nginx
sudo systemctl reload nginx
```

---

## 📅 История изменений

- **2024-11** - Создание единого руководства, объединение 50+ скриптов
- **2024-11** - Добавлена поддержка multi-server deployment
- **2024-11** - Добавлена настройка Nginx Load Balancer

---

**Версия:** 1.0  
**Дата:** Ноябрь 2024  
**Проект:** LiveKit Stream Application


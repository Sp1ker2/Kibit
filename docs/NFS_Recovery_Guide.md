# 🧰 Руководство: что делать при сбоях NFS/хранилища

## 1. Как распознать проблему

- `ls` / `df` на `/www/wwwroot/LiveKit/recordings` «зависают» или команды сами не возвращаются.
- Load average на приложениях (`server1–4`) растёт до десятков, хотя CPU почти свободен; в `ps` видно много процессов в состоянии `D` (`ls`, `df`, `nfsd`).
- `ssh` на storage (`195.133.39.41`) отвечает с ошибками или вообще недоступен.
- В aaPanel / File Manager бесконечно висит «Retrieving file list…».

## 2. Действия на приложениях (server1–4) — сразу

1. **Остановить обход каталога.** Закрыть вкладки aaPanel, прекратить `ls -R` / `df` по `recordings`.
2. На каждом app-сервере:
   ```bash
   pkill -f "/www/wwwroot/LiveKit/recordings"
   ```
   Это снимает зависшие `ls`/`df`, чтобы не держать монтирование.
3. Не выполнять новых команд по каталогу, пока storage не проверен.

## 3. Проверка storage-сервера (`195.133.39.41`)

1. Подключиться: `ssh root@195.133.39.41`.
2. Проверить NFS:
   ```bash
   systemctl status nfs-server
   ls /www/wwwroot/LiveKit/recordings | head
   ```
3. Если команды зависают или `ssh` не подключается — сервер вероятно выключен/завис. **Нужен доступ в панель провайдера (Litnets)**:
   - Включить сервер (`Power on`) или сделать `Reset`.
   - После старта — `bt status`, `systemctl status nfs-server`.

## 4. Восстановление после перезагрузки storage

1. Убедиться, что NFS работает:
   ```bash
   systemctl status nfs-server
   showmount -a
   ```
2. На приложениях (через Ansible или вручную):
   ```bash
   umount -f /www/wwwroot/LiveKit/recordings   # если смонтирован старый сеанс
   mount /www/wwwroot/LiveKit/recordings
   ```
3. Проверить:
   ```bash
   mount | grep recordings
   ```
4. Убедиться, что API/LiveKit стартовали:
   ```bash
   systemctl is-active streamapp-api.service
   curl -I https://kibitkostreamappv.pp.ua/api/room-list
   ```

## 5. Профилактика

- **Не запускать** `ls -R`, `df -hT -P`, `du` без ограничений по глубине на NFS. Используйте:
  ```bash
  ls /www/wwwroot/LiveKit/recordings | head
  find /www/wwwroot/LiveKit/recordings -maxdepth 2 -type d
  ```
- Для больших проверок применять `ansible`/`rsync` с фильтрами, а не интерактивные обходы.
- Держать доступ к панели провайдера; без него невозможно поднять storage.
- Регулярно переносить записи на резервное хранилище, чтобы объём каталога NFS оставался разумным.

## 6. Если NFS повторно выходит из строя

1. Выполнить шаги из разделов 2–4.
2. Рассмотреть альтернативу: локальное хранение, объектное хранилище (S3) или синхронизацию через Ansible без NFS.
3. Обновить документацию и Ansible playbooks, если переходите на другую схему хранения.

---

> **Важно:** компания Litnets — единственный способ включить сервер, если он выключен. Всегда храните доступ к их панели и E-mail для сброса пароля.




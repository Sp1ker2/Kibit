#!/bin/bash
# Просмотр содержимого базы данных

DB_PATH="streamApp/database.db"

echo "📊 Просмотр базы данных SQLite"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

if [ ! -f "$DB_PATH" ]; then
    echo "❌ База данных не найдена: $DB_PATH"
    exit 1
fi

echo "✅ База данных найдена: $DB_PATH"
echo ""

# Проверяем что sqlite3 установлен
if ! command -v sqlite3 &> /dev/null; then
    echo "❌ sqlite3 не установлен!"
    echo "   Установите: sudo apt-get install sqlite3"
    exit 1
fi

echo "📋 Структура таблицы users:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
sqlite3 "$DB_PATH" ".schema users"
echo ""

echo "👥 Пользователи в базе данных:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
sqlite3 -header -column "$DB_PATH" "SELECT id, username, password, created_at FROM users ORDER BY id;"
echo ""

echo "📊 Статистика:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
sqlite3 "$DB_PATH" "SELECT COUNT(*) as 'Всего пользователей' FROM users;"
echo ""




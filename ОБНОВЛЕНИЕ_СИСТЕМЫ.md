# 🎯 Обновление системы стриминга

## ✅ Что сделано:

### 1. **Автоматическое сохранение записей по 5 минут**
- Каждые 5 минут стрим автоматически сохраняется
- При остановке стрима последняя часть тоже сохраняется
- Файлы имеют формат: `username_timestamp_part1.webm`

### 2. **Структура папок для записей**
```
recordings/
├── User1/
│   ├── 2025-11-05/
│   │   ├── User1_1730835600_part1.webm (0-5 мин)
│   │   ├── User1_1730835900_part2.webm (5-10 мин)
│   │   └── User1_1730836200_part3.webm (10-15 мин)
│   └── 2025-11-06/
│       └── User1_1730922000_part1.webm
└── User2/
    └── 2025-11-05/
        └── User2_1730835700_part1.webm
```

### 3. **Выбор комнаты при входе**
- При входе пользователь выбирает комнату из списка
- Комната отображается в шапке стрима
- Информация о комнате сохраняется в сессии

### 4. **Управление комнатами (Админ)**
- В админ-панели есть вкладка "Комнаты"
- Можно создавать новые комнаты
- Можно удалять существующие комнаты

## 🚀 Как использовать:

### Первый запуск:

1. **Запустите API сервер:**
```bash
cd /Users/bogdanprihodko/Downloads/LiveKit/streamApp
npm run dev
```

2. **База данных инициализируется автоматически**
   - Создаются 3 комнаты по умолчанию:
     - Комната 1
     - Комната 2  
     - Комната 3
   - Создаются пользователи: Admin, User

### Для обычного пользователя:

1. **Откройте приложение**
2. **Выберите комнату из списка** 📍
3. **Введите логин и пароль**
4. **Запустите стрим** 
5. **Каждые 5 минут** запись автоматически сохраняется

### Для админа:

1. **Войдите как Admin** (логин: Admin, пароль: Admin)
2. **Вкладка "Комнаты"** - управление комнатами
3. **Вкладка "Аккаунты"** - управление пользователями
4. **Вкладка "Записи"** - просмотр всех записей
5. **Вкладка "Live"** - активные стримы

## 🗄️ База данных:

### Таблица `users`:
- id
- username (логин)
- password
- role (admin/user)
- created_at

### Таблица `rooms`:
- id
- name (название комнаты)
- description (описание)
- created_at

## 📝 API Endpoints:

### Комнаты:
- `GET /api/room-list` - получить список комнат
- `POST /api/room-list` - создать комнату
- `DELETE /api/room-list/:id` - удалить комнату

### Пользователи:
- `POST /api/auth/login` - авторизация
- `GET /api/users` - список пользователей
- `POST /api/users` - создать пользователя
- `PUT /api/users/:id` - обновить пользователя
- `DELETE /api/users/:id` - удалить пользователя

### Записи:
- `GET /api/recordings` - список всех записей (рекурсивно)
- `POST /api/recordings/upload` - загрузить запись
- `GET /api/recordings/stream/:path(*)` - стримить запись
- `GET /api/recordings/download/:path(*)` - скачать запись

### LiveKit:
- `GET /api/rooms` - активные стримы

## ⚙️ Настройки:

### Изменить интервал записи:

В файле `streamApp/src/components/user/UserStreamPage.tsx`, строка ~169:

```typescript
}, 5 * 60 * 1000) // 5 минут
```

Измените на нужное значение:
- 3 минуты: `3 * 60 * 1000`
- 10 минут: `10 * 60 * 1000`
- 1 минута: `1 * 60 * 1000`

### Добавить комнату через SQL:

```sql
INSERT INTO rooms (name, description) VALUES ('Моя комната', 'Описание');
```

## 🎨 Интерфейс:

### Страница входа:
- ✅ Выбор комнаты (select)
- ✅ Логин
- ✅ Пароль

### Стрим пользователя:
- ✅ Отображение комнаты (badge)
- ✅ Отображение пользователя (badge)
- ✅ Автосохранение каждые 5 минут

### Админ-панель:
- ✅ Live - активные стримы
- ✅ Записи - все записи по папкам
- ✅ Аккаунты - управление пользователями
- ✅ Комнаты - управление комнатами (НОВОЕ!)

## 📦 Файлы которые были изменены:

1. `streamApp/server/db.js` - добавлена таблица rooms
2. `streamApp/server/api.js` - API для комнат, обновлены записи
3. `streamApp/src/App.tsx` - сохранение room в localStorage
4. `streamApp/src/components/LoginForm.tsx` - выбор комнаты
5. `streamApp/src/components/user/UserStreamPage.tsx` - автосохранение по 5 мин
6. `streamApp/src/components/admin/AdminDashboard.tsx` - страница комнат
7. `streamApp/src/components/admin/AdminHeader.tsx` - пункт меню "Комнаты"
8. `streamApp/src/components/admin/RoomsManagementPage.tsx` - управление комнатами (НОВЫЙ!)
9. `streamApp/src/components/admin/RecordingsPage.tsx` - работа с новой структурой
10. `streamApp/src/components/admin/RecordingPlayer.tsx` - работа с новой структурой

## 🔥 Готово к использованию!

Просто запустите сервер и всё будет работать:

```bash
cd /Users/bogdanprihodko/Downloads/LiveKit/streamApp
npm run dev
```

Откройте браузер и войдите в систему! 🎉


# 🔧 Что делать с проблемой копирования на server4

## Вариант 1: Настроить SSH ключи (РЕКОМЕНДУЕТСЯ) ✅

Это решит проблему раз и навсегда:

```bash
cd ansible
./setup-ssh-keys.sh
```

Этот скрипт:
- ✅ Создаст SSH ключ если его нет
- ✅ Скопирует ключ на все серверы
- ✅ После этого можно убрать пароли из `inventory.ini`

**После настройки ключей:**
- Обновите `inventory.ini` - уберите `ansible_ssh_pass` строки
- Или оставьте их как fallback

## Вариант 2: Оставить как есть (уже работает) ✅

С `ignore_errors: yes` playbook уже работает:
- ✅ Не падает при ошибке копирования на server4
- ✅ Копирует файлы на остальные серверы
- ⚠️ Для server4 нужно копировать вручную при необходимости

**Копирование вручную на server4:**
```bash
cd /Users/bogdanprihodko/Downloads/LiveKit
sshpass -p 'vHdUm7B2' rsync -avz --exclude=node_modules --exclude=.git --exclude='*.log' streamApp/ root@195.133.39.33:/www/wwwroot/LiveKit/streamApp/
```

## Вариант 3: Использовать модуль copy (альтернатива)

Можно заменить `synchronize` на `copy` в playbook - это будет работать с паролями, но медленнее для больших файлов.

## 🎯 Рекомендация

**Используйте Вариант 1 (SSH ключи)** - это стандартный и безопасный способ, который решит проблему полностью.

После настройки ключей:
1. Playbook будет работать без ошибок
2. Не нужно будет вводить пароли
3. Безопаснее чем пароли в файле

## 📝 Быстрый старт

```bash
cd ansible
./setup-ssh-keys.sh
```

Готово! 🎉


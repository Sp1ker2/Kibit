# 🎬 LiveKit Desktop Recorder - Инструкция по сборке

## 📦 ЧТО ДЕЛАЕТ ЭТО ПРИЛОЖЕНИЕ:

✅ **Захватывает ВСЕ физические мониторы**
✅ **Склеивает их в одно видео** (например: 2 монитора 1920×1080 → видео 3840×1080)
✅ **Автоматически видит виртуальные рабочие столы Windows**
✅ **Работает в фоне** (сворачивается в системный трей)
✅ **Автосохранение каждые 5 минут** на сервер

---

## 🔧 ТРЕБОВАНИЯ:

### На Windows ПК нужен:
1. **Python 3.10+** - https://www.python.org/downloads/
   - ⚠️ При установке поставить галочку: `☑ Add Python to PATH`
2. **Microsoft Visual C++ Redistributable** (для OpenCV)
   - https://aka.ms/vs/17/release/vc_redist.x64.exe

---

## 📋 ШАГИ СБОРКИ:

### **1. Распаковать архив**

```
PythonRecorderApp.zip
    ↓
Правой кнопкой → "Извлечь в PythonRecorderApp\"
```

### **2. Открыть папку**

```
PythonRecorderApp\
  ├── BUILD.bat         ← Запустить это
  ├── recorder.py       ← Исходный код
  ├── requirements.txt  ← Зависимости
  ├── build.spec        ← Конфигурация PyInstaller
  └── README.md
```

### **3. Двойной клик на BUILD.bat**

Скрипт автоматически:
1. Проверит Python
2. Установит зависимости
3. Соберёт EXE
4. Положит в папку `dist\`

### **4. Забрать готовый EXE**

```
dist\LiveKitRecorder.exe  ← ВОТ ОН!
```

---

## 🎯 КАК ИСПОЛЬЗОВАТЬ:

### **1. Запустить LiveKitRecorder.exe**

### **2. Войти**
- Выбрать комнату
- Ввести логин/пароль

### **3. Выбрать экраны**

**Варианты:**

**A) Один монитор:**
```
☑ Экран 1 (1920×1080)
☐ Экран 2 (1920×1080)

→ Видео: 1920×1080
→ Видит все виртуальные столы на Экране 1
```

**B) Два монитора:**
```
☑ Экран 1 (1920×1080)
☑ Экран 2 (1920×1080)

→ Видео: 3840×1080 (склеены горизонтально)
→ Видит все виртуальные столы на ОБОИХ экранах
```

**C) Три монитора:**
```
☑ Экран 1 (1920×1080)
☑ Экран 2 (1920×1080)
☑ Экран 3 (1920×1080)

→ Видео: 5760×1080 (все три склеены)
```

### **4. Начать запись**

Нажать "▶ Начать запись"

### **5. Свернуть в трей**

Нажать "↓ Свернуть в трей" → иконка появится в трее

**Запись продолжается в фоне!**

### **6. Переключать виртуальные столы**

```
Win + Tab → Desktop 1 → Desktop 2 → Desktop 3

ВСЁ это будет в записи! ✅
```

### **7. Автосохранение**

Каждые **5 минут** видео автоматически загружается на сервер

### **8. Остановить**

Правой кнопкой на иконку в трее → "Показать окно" → "⏹ Остановить запись"

---

## 🔍 КАК ЭТО РАБОТАЕТ ПОД КАПОТОМ:

### **1. Захват экрана - библиотека `mss`**

```python
with mss.mss() as sct:
    # Захватываем монитор
    sct_img = sct.grab(monitor)
    frame = np.array(sct_img)
```

`mss` использует:
- **Windows:** DXGI Desktop Duplication API
- Самый быстрый метод захвата (60+ FPS)
- Видит ВСЁ, что на физическом мониторе

### **2. Склейка мониторов - NumPy**

```python
frames = []
for monitor in monitors:
    img = sct.grab(monitor)
    frames.append(np.array(img))

# Склеиваем горизонтально
combined = np.hstack(frames)
```

**Пример:**
```
Монитор 1: [1920×1080]  Монитор 2: [1920×1080]
        ↓                       ↓
     frame1                  frame2
        ↓                       ↓
        └───── hstack ─────────┘
                   ↓
          [3840×1080] ← цельная картинка
```

### **3. Кодирование видео - OpenCV**

```python
fourcc = cv2.VideoWriter_fourcc(*'mp4v')
video_writer = cv2.VideoWriter(
    filename, fourcc, 30.0, (width, height)
)

video_writer.write(frame)  # Пишем каждый кадр
```

### **4. Виртуальные столы**

Windows виртуальные столы = просто **показ/скрытие окон**

```
Физический монитор = один
    ↓
Desktop 1: показывает окна [A, B, C]
Desktop 2: показывает окна [D, E, F]

Переключение Desktop 1 → Desktop 2:
    ↓
Windows просто СКРЫВАЕТ [A,B,C] и ПОКАЗЫВАЕТ [D,E,F]
    ↓
mss захватывает монитор → видит [D,E,F] ✅
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ:

### **Ошибка: "Python не найден"**

Установить Python с галочкой "Add to PATH"

### **Ошибка: "requirements.txt not found"**

Запускать BUILD.bat из **распакованной папки**, не из архива!

### **Ошибка: "cv2 import failed"**

Установить Visual C++ Redistributable

### **Запись останавливается при переключении столов**

Это невозможно с `mss`! Он захватывает физический монитор, видит все столы.

Если останавливается → это другая причина (например, антивирус блокирует).

---

## 📊 ТЕХНИЧЕСКИЕ ДЕТАЛИ:

### **Формат видео:**
- Кодек: MP4V (H.264)
- FPS: 30
- Разрешение: автоматически (зависит от мониторов)

### **Размер файлов:**
- ~50-100 MB за 5 минут (зависит от разрешения)

### **Системные требования:**
- Windows 10/11
- Python 3.10+
- 4+ GB RAM
- Диск: ~1 GB/час записи

---

## ✅ ИТОГО:

1. **Распаковать** `PythonRecorderApp.zip`
2. **Установить** Python 3.10+ с галочкой "Add to PATH"
3. **Запустить** `BUILD.bat`
4. **Получить** `dist\LiveKitRecorder.exe`
5. **Выбрать** все нужные экраны
6. **Свернуть** в трей
7. **Переключать** виртуальные столы — **ВСЁ будет в записи!** 🔥

---

## 🎯 ГЛАВНОЕ:

**Приложение УЖЕ делает то, что тебе нужно:**
- ✅ Захватывает все экраны
- ✅ Склеивает их в одно видео
- ✅ Видит виртуальные столы
- ✅ Работает в фоне

**Виртуальные столы Windows — это не отдельные "экраны"!**
Это просто способ организации окон на физических мониторах.
Любое приложение, которое захватывает физический монитор, автоматически видит все виртуальные столы! 🎯


# 🌐 Доступ из локальной сети

Приложение настроено для доступа из локальной сети. Другие устройства (телефоны, планшеты, компьютеры) в той же Wi-Fi сети смогут подключиться к вашему серверу.

## 🚀 Запуск

```bash
./start.sh
```

После запуска вы увидите:
```
📱 Доступ из локальной сети:
   👉 http://192.168.X.X:5173
```

## 📱 Как подключиться с других устройств

### 1. Узнайте свой IP адрес

**macOS:**
```bash
ipconfig getifaddr en0
# или
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**Linux:**
```bash
hostname -I | awk '{print $1}'
```

**Windows:**
```cmd
ipconfig | findstr IPv4
```

### 2. Откройте приложение на другом устройстве

На телефоне/планшете/другом компьютере в той же Wi-Fi сети откройте браузер и введите:

```
http://ВАШ_IP_АДРЕС:5173
```

Например:
- `http://192.168.1.100:5173`
- `http://192.168.0.50:5173`

## 🔧 Порты

Приложение использует следующие порты:
- **5173** - React приложение (фронтенд)
- **3001** - API сервер (бэкенд)
- **7880** - LiveKit сервер (WebRTC)

## 🔒 Firewall

Если не можете подключиться с других устройств, возможно нужно открыть порты в firewall:

**macOS:**
```bash
# Обычно firewall не блокирует, но если нужно:
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/node
```

**Linux:**
```bash
sudo ufw allow 5173
sudo ufw allow 3001
sudo ufw allow 7880
```

## ⚠️ Важно

1. **Одна Wi-Fi сеть**: Все устройства должны быть в одной Wi-Fi сети
2. **IP может измениться**: После перезагрузки роутера IP адрес может измениться
3. **Не для интернета**: Это настройка только для локальной сети, не для публичного доступа через интернет

## 🧪 Проверка подключения

С другого устройства попробуйте:

```bash
ping ВАШ_IP_АДРЕС
```

Если ping работает, но приложение не открывается - проверьте firewall.



# 🎬 LiveKit Stream Recorder

Desktop приложение для захвата и стриминга нескольких мониторов одновременно.

## ✨ Возможности

- 📺 **Захват 2+ мониторов одновременно**
- 🎥 **Live стриминг через LiveKit**
- 💾 **Автосохранение каждые 5 минут**
- 🔐 **Авторизация через API**
- 📊 **Просмотр через браузер**

## 🚀 Запуск

### Для пользователей:
1. Скачайте `LiveKit-Stream-Recorder-Portable.exe`
2. Запустите файл
3. Введите логин, пароль и выберите комнату
4. Выберите мониторы для захвата
5. Нажмите "Запустить стрим"

### Для разработчиков:

```bash
npm install
npm start
```

## 🔨 Сборка

```bash
npm run build          # Windows exe
npm run build-all      # Windows + Mac + Linux
```

## 📁 Структура

- `main.js` - Главный процесс Electron
- `renderer.js` - Логика захвата и стриминга
- `index.html` - Интерфейс приложения

## 🌐 API

Приложение использует API по адресу: `https://kibitkostreamappv.pp.ua`

## 🔧 Технологии

- Electron
- LiveKit Client
- MediaRecorder API
- Canvas API (для объединения мониторов)

## 📝 Как это работает

1. **Захват** - через `desktopCapturer` получаем список мониторов
2. **Объединение** - если выбрано 2+ монитора, объединяем через Canvas
3. **Запись** - через MediaRecorder сохраняем видео
4. **Стриминг** - через LiveKit отправляем live поток
5. **Автосохранение** - каждые 5 минут отправляем файл на сервер

## 🎯 Структура папок на сервере

```
recordings/
  └── room_name/
      └── username/
          └── YYYY-MM-DD/
              ├── video_1.webm
              ├── video_2.webm
              └── ...
```

## 🛠️ Troubleshooting

**Не видит мониторы?**
- Перезапустите приложение с правами администратора

**Не подключается к серверу?**
- Проверьте интернет соединение
- Убедитесь что API доступен: https://kibitkostreamappv.pp.ua

**Ошибка при сохранении?**
- Проверьте место на диске
- Проверьте права доступа

## 📞 Поддержка

При возникновении проблем обратитесь к администратору.

---

**Версия**: 1.0.0
**Дата**: Ноябрь 2025


